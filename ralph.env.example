# Ralph Configuration
#
# Config locations (in priority order):
#   1. CLI arguments
#   2. Environment variables  
#   3. .ralph/ralph.env (project config)
#   4. ~/.config/ralph/ralph.env (global config)
#
# This file is an example. To use:
#   - For project config: mkdir -p .ralph && cp ralph.env.example .ralph/ralph.env
#   - For global config: cp ralph.env.example ~/.config/ralph/ralph.env

# ─────────────────────────────────────────────────────────────
# Engine Selection
# ─────────────────────────────────────────────────────────────

# Which AI engine to use: "opencode" or "claude"
ENGINE=opencode

# ─────────────────────────────────────────────────────────────
# Model Settings
# ─────────────────────────────────────────────────────────────

# OpenCode model (default: big-pickle - free tier)
OPENCODE_MODEL=big-pickle

# Claude model (default: sonnet - balances cost/capability)
# Options: opus, sonnet, haiku
CLAUDE_MODEL=sonnet

# Fallback model when primary hits rate limits (optional)
# Leave empty or comment out to disable fallback
# FALLBACK_MODEL=

# ─────────────────────────────────────────────────────────────
# Iteration Settings
# ─────────────────────────────────────────────────────────────

# Maximum iterations before stopping
# Use -1 for infinite (run until all tasks complete)
MAX_ITERATIONS=-1

# Sleep seconds between iterations
SLEEP_SECONDS=2

# ─────────────────────────────────────────────────────────────
# Behavior Settings
# ─────────────────────────────────────────────────────────────

# Skip commits during runs (useful for test PRDs)
# Set to 1 to disable commits
SKIP_COMMIT=0

# Skip test verification entirely (not recommended)
# Set to 1 to disable test checks
SKIP_TEST_VERIFY=0

# ─────────────────────────────────────────────────────────────
# Test Settings
# ─────────────────────────────────────────────────────────────

# Test command (auto-detected if not set)
# Examples: "npm test", "bun test", "pytest", "go test ./..."
TEST_CMD=

# ─────────────────────────────────────────────────────────────
# Logging Settings
# ─────────────────────────────────────────────────────────────

# Log directory (default: ~/.ralph/logs)
# Logs are named ralph-<projectname>.log
RALPH_LOG_DIR=
