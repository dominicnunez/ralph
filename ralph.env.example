# Ralph Configuration
# Copy this file to ralph.env and customize as needed.
# Values here are defaults - CLI arguments override these settings.

# ─────────────────────────────────────────────────────────────
# General Settings
# ─────────────────────────────────────────────────────────────

# Maximum iterations before stopping
# Use -1 for infinite (run until all tasks complete)
MAX_ITERATIONS=-1

# Sleep seconds between iterations
SLEEP_SECONDS=2

# Skip commits during runs (useful for test PRDs)
# Set to 1 to disable commits
SKIP_COMMIT=0

# ─────────────────────────────────────────────────────────────
# Claude Settings (for ralphcc.sh)
# ─────────────────────────────────────────────────────────────

# Claude Code model
# Options: opus, sonnet, haiku
CLAUDE_MODEL=opus

# ─────────────────────────────────────────────────────────────
# OpenCode Settings (for ralphoc.sh)
# ─────────────────────────────────────────────────────────────

# Available Models:
# Free:
# OpenCode:
#   opencode/big-pickle
#   opencode/glm-4.7-free
#   opencode/gpt-5-nano,
#   opencode/grok-code
#   opencode/minimax-m2.1-free

# Paid:
# Anthropic:
#   anthropic/claude-opus-4-5
#   anthropic/claude-sonnet-4-5
#   anthropic/claude-haiku-4-5
# OpenAI:
#   openai/gpt-5.2
#   openai/gpt-5.2-codex

# Primary model (provider/model format)
PRIMARY_MODEL=anthropic/claude-opus-4-5

# Reasoning variant for primary model (optional)
# Codex Options: low, medium, high, xhigh
# Claude Options: high, max
PRIMARY_MODEL_REASONING=

# Fallback model when primary hits rate limits or quota
# Leave empty to disable fallback
FALLBACK_MODEL=openai/gpt-5.2-codex

# Reasoning variant for fallback model (optional)
FALLBACK_MODEL_REASONING=xhigh

# ─────────────────────────────────────────────────────────────
# Test Verification Settings
# ─────────────────────────────────────────────────────────────

# Test command (auto-detected if not set)
# Examples: "npm test", "bun test", "pytest", "go test ./..."
TEST_CMD=

# Skip test verification entirely (not recommended)
# Set to 1 to disable test checks
SKIP_TEST_VERIFY=0

# ─────────────────────────────────────────────────────────────
# Logging Settings  
# ─────────────────────────────────────────────────────────────

# Log directory (default: ~/.ralph/logs)
# Logs are named ralph-<projectname>.log
RALPH_LOG_DIR=
